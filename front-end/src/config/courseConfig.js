/**
 * Course Configuration
 * Maps assessment stage keys to their corresponding course IDs.
 * Centralised here so hardcoded IDs only live in one place.
 */
export const STAGE_COURSE_MAP = {
  T2: 'S11',
  T3: 'S20',
};
